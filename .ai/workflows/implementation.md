# Implementation Workflow

`要求確認 → 関連ドキュメント → 検索 → 対象特定 → 影響範囲確認 → 最小実装 → Fast Validation → Full Validation → 必要なDocs更新`

- 受け入れ条件と非対象を先に確認する。
- 検索結果から対象、参照元、境界、関連テストを絞る。
- 無関係な変更を混ぜず、要求を満たす最小差分にする。
- `TESTING.md`の確立済み手順を使い、未実行・失敗は理由とともに明示する。
- 振る舞いや判断が変わった場合だけ該当する正本・ADRを更新する。
